import 'package:flutter/material.dart';

class BindampingRegistScreen extends StatelessWidget {
  const BindampingRegistScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Bindamping Regist"),
      ),
    );
  }
}
